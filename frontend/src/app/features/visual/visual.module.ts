import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { VisualRoutingModule } from './visual.routing.module';

@NgModule({
  imports: [CommonModule, VisualRoutingModule],
})
export class VisualModule {}
